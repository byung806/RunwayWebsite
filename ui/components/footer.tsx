import Link from 'next/link';
import NavLinks from '@/ui/components/nav-links';
import WhizworksLogo from '@/ui/components/logo';

export default function Footer() {
    return (
        <footer className="flex items-center justify-center h-24 bg-gray-500">
            <p>©</p>
        </footer>
    );
}
